import styled from "styled-components";

const StyledTopNav = styled.div`
    font-weight: 700;
    color: white;
    display: flex;
    justify-content: row;
    margin: auto;
`;

export default StyledTopNav