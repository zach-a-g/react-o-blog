import styled from "styled-components";

const Title = styled.h1`
    font-weight: 700;
    color: white;
    text-align: left;
    width: 95vw;
    display: flex;
    margin: auto;
    padding-bottom: 10px
`;

export default Title